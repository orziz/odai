# 子 agent 统一执行包

## 运行身份

- 当前父流程：odai
- 父流程任务 ID / session 标识：
- 当前模式：增强模式（完整多模型合议） | 当前模型三席模拟复核 | 子 agent 辅助复核 | 原流程执行分工
- 当前能力说明：用户真实可用模型列表已暴露 | 仅确认当前用户当前模型三席模拟可用 | 模型列表未暴露 | 仅确认子 agent 可用
- 模型信息来源：运行环境直出 / 当前工具面直接返回 / 当前会话日志或模型缓存 / 宿主同会话回填 | 未暴露
- 自查与续查状态：已本地自查 | 已 AI 续查 | 已回交总控续查 | 已通知宿主/API 续查 | 仍未暴露
- 当前阶段：确认前题面校验 | 确认后再分析 | 首轮合议 | 定向复议 | 冻结后独立复查 | 执行分工
- 本席位关注点：
- 执行分工填写：若当前阶段是“执行分工”，这里改填本席位分工范围 / 负责产物。
- 本席位 agent：
- 本席位模型：
- 本席位模型选择理由：
	- 若当前已命中仓库模型倾向，优先按：视觉 / 文案 -> Gemini，分析 / 规划 -> Claude，验收 / 执行 -> ChatGPT；若未采用该倾向，必须写明是被异构性、席位关注点，还是成本 / 权限 / 配额 / 派位拒绝覆盖。
- 阶段约束：
	- 若当前阶段属于确认前题面校验 / 确认后再分析 / 首轮合议 / 定向复议 / 冻结后独立复查，所有席位都必须围绕同一冻结题面与同一核心问题给出相对完整判断；关注点只用于额外挑战，不用于把讨论/决策阶段拆成分工接力。
	- 若当前模式是“增强模式（完整多模型合议）”且当前阶段仍属于执行前的讨论 / 决策链路，主流程分配给全部并行席位的模型组必须至少覆盖 3 种当前用户真实可用的不同模型；其中最多 1 个席位可与主流程同模型，不得用同模型副本或只换 agent 名称凑数。
	- 若当前模式是“当前模型三席模拟复核”，主流程分配给全部并行席位的模型组可以共用当前模型或当前环境同路默认模型，但必须固定为 3 个席位，并至少覆盖反证、边界 / 风险、证据 / 成本三种不同关注点；该模式不得写成多模型参与。
	- 若本席位被指定为反证 / 逆向挑战关注点，就必须优先挑战暂定主流判断，尽力寻找能推翻它的反例、边界缺口或证据缺口；若未找到，也要明确写出已挑战但未推翻，不得为了形式硬造一个无依据的相反结论。
	- 若当前阶段属于执行分工，各席位可以按已冻结方案分工，但必须共同承接同一冻结题面、已定主路、边界与验收；不得擅自改题面、改主路、改边界，或把执行分工包装成增强模式。
- 本包说明：本下发包只对应 1 个子 agent 席位；若有多个席位，主流程必须分别生成多个下发包。

## 必读文件

- canonical 入口：skills/odai/SKILL.md
- 当前命中模块：
- 必读 support files：

## 当前理解

- 当前理解：
- 当前理解的用户诉求：
- 当前已确认的用户真实想法与在意点：

## 事实与边界

- 已知事实：
- 已验证事实：
- 未确认点：
- 冲突点：
- 当前题面：阶段零填写“问题定义草案（未经用户确认）”；确认后阶段与执行分工阶段填写“冻结题面”。
- 范围：
- 非范围：
- 成功标准：
- 不可接受结果：
- 禁止擅自假设：

## 本席位任务

- 本席位要回答的问题：
- 本席位重点校验：
- 允许使用的证据：
- 不允许做的事：不得自行改题面、不得替主流程冻结方案、不得自行调起其他 agent、不得替主流程升级给用户。

## 输出要求

- 输出结构：
- 若当前阶段属于合议 / 复核阶段，必须围绕同一核心问题给出相对完整判断；本席位关注点只用于补充挑战，不得把本席位输出包装成最终裁决，也不得把合议做成分工接力。
- 若本席位是反证 / 逆向挑战席位，必须显式写出自己挑战的暂定判断、挑战依据、挑战结果，以及是否真的推翻了暂定共识；若未推翻，也必须如实回报，不得为满足流程伪造相反结论。
- 若当前阶段属于执行分工，必须在已冻结方案和本席位范围内推进，并显式回报完成情况、风险与需回写事项；不得把本席位输出包装成最终裁决，也不得擅自改写既定主路、边界或验收。
- 若当前模式是“当前模型三席模拟复核”，不得把自己的分析写成“多模型合议”“多模型一致结论”或“完整增强模式结论”。
- 若当前模式是“子 agent 辅助复核”，不得把自己的分析写成“多模型合议”或“模型间一致结论”。
- 若本席位模型来自当前运行环境直接返回、当前工具面直接返回、当前会话日志 / 模型缓存或宿主同会话回填的稳定标识，结果里应沿用该稳定标识；若当前环境仍未暴露，则明确写“当前环境未暴露”。
- 若发现会改写路线、边界、验收或不可接受结果的关键不确定，必须显式指出并回交主流程。
- 若无法确认自己已承接同版 odai skill 契约，立即返回：无法确认已承接同版 odai skill 契约，请主流程回退原流程。
- 不转储过程性争论，不虚构已读文件、已调用 agent 或已使用模型。