# Odai DSH 路由验证

当前已发布双包为 DSH `0.2.37`（治理 `0.7.0` / contract `9`、编排 `0.2.0`），采用 rc.2 合同；源码候选为未发布 `0.2.38`，治理 `0.7.1` / runtime contract `9`，编排 `0.2.0`，npm peer 开放为 `*`，Agent 允许 `>=0.1.7-rc.1`（含后续预发布），仅 `0.1.7-rc.1` 已实测。使用与配置合同以 [`dsh/README.md`](../dsh/README.md) 为准，当前任务评分见 [`evaluation-results.md`](evaluation-results.md)。本页只保留当前有用的证据及限制；旧版本实验过程由 Git 历史承载。

## DSH 0.1.7-rc.1 适配验证

上游 `0.1.7-rc.1` 把 agent 预设改为由 profile bundle 声明（`@deepseek-ai/dsh-agent-preset` 与 `dsh-agent-preset-registry`），宿主不再扫描 `.agent-presets`；Standard 由 `dsh-web-app/presets/standard.patch.yml` 提供，工作流引擎改为 `dsh-workflow-ptc`，`ralph` 与插件管理工具默认关闭。`0.2.38` 的 Agent 改为 bundle，以 `preset.cordis.patch.yml` 声明 `odai` 预设，与 Control Center 一起经插件管理器安装；Plugin 运行时代码未改。

在独立临时安装与临时 `DSH_HOME` 中完成（未修改上游包、正在运行的宿主或既有用户配置）：

- 开放 peer 后重新运行发布矩阵源码模式通过，其中包含双包的实际打包、安装与共存：267 个 DSH 包构成纯 `0.1.7-rc.1` 依赖图，Standard 摘要 `6cd2f197…eda8`，历史会话不改写、Plugin 加载、Agent 生产安装路径通过。本轮没有另外重跑独立的 packed 模式，不将较早报告回填为最终制品的重复实测。
- 构建、类型检查、Agent 安装与发布策略共 44 项回归通过，canonical 和版本一致性校验通过。缺失、无效映射和必需文件变成目录均进入 `partial-drift` 修复；默认项覆盖 `default`、`selectedDefault`、关闭选择器与 home 覆盖，动态值不能安全判定时拒绝卸载；未来正式版与预发布版本不因不在列表而拒绝。矩阵之后补充的损坏路径读取异常处理只重跑受影响回归，复用未受影响的正常安装与宿主证据。
- Agent 真实宿主探针：roster 含 `odai`；Standard 会话不注入 Odai，`odai` 会话恰好注入一次；子代理写入被拦截，路由保护与工具暴露同步；通过设置接口把 `odai` 设为默认后，profile `cordis.patch.yml` 以 `agent-preset-registry.selectedDefault` 保存。修复后的卸载保护改用宿主 `--dump-config` 合并结果，覆盖部署默认值、选择器关闭以及 home 层覆盖，无法静态判定时拒绝；不再只读 profile 的 `selectedDefault`。
- 双包共存：两个客户端各一次，Control Center RPC 可用；Standard 会话由全局 Plugin 注入内置治理，`odai` 会话由 bundle 预设注入并选中项目技能，均为一次。

未覆盖：真实模型委派、工作流任务执行与付费冒烟；评测 DSH runner 仍依赖复制的预设目录，在 `0.1.7` 上跑 Agent 组会明确报错。上游旧 `settings.yaml` 导入不映射 `agent-presets.default`，升级后默认预设回到 Standard，需要用户在 DSH 中重新选择。

## 当前结构与验证边界

`skills/odai/SKILL.md` 独立提供治理，manifest schema 5 / runtime contract 9 不含角色或编排依赖。可安装的 `skills/odai-orchestration/` 单向消费治理，持有受托边界、角色、交接规则和宿主生成器。DSH 内置两份来源，分别计算身份并绑定同一组合快照；调度、权限与实际调用证据仍由 runtime 承担。旧来源不自动迁移，无法加载时明示降级并保留原数据；技能演进已退役，`$DSH_HOME/odai/skill-evolution` 的既有数据不再读取，也不会被删除。

Markdown 不再做逐句、关键词、词序或语义反转测试，也不设篇幅审定基线。基本检查只确认元数据、文件、引用和合同可以加载；内容是否合适须结合用户目标与全文判断。DSH 可执行行为继续由对应测试验证，不能把这些结果写成模型理解或协作收益证明。

`0.2.38` / 治理 `0.7.1` 已通过 `check:dsh`、Plugin 与路由上下文、治理组合与来源选择、版本策略与发布入口共 141 项回归，canonical 资源校验，以及评测设施离线自检与 13 项测试。回归覆盖完整委派范围之后的用户原文截断可派发、范围本身被截断则拒绝、未启动的 fallback 不会导致按次数拒绝新请求，以及审查其他属性后仍可记录新的有据缺口。撤回两次硬上限后，tool-runtime、responsibility-gap 与 index 的源码同 `655623d`；既有已消费请求去重保留，不承诺原样重提失败请求会再次派发。调用失败后仍须有新的可核查修正依据，证据包尚未齐备的请求则保持 pending。

与 npm `0.2.37` 的治理 `0.7.0` 制品比较，`0.7.1` 用户来源在 `user`、`auto` 两种模式均实际选中，不再触发同版本内容冲突。精确 DSH `0.1.5-rc.2` 发布矩阵重新通过，覆盖隔离依赖图、Plugin 加载、Agent 权限和动态工具暴露、双包 `0.2.38` 实际打包与共存；未修改上游 DSH 或用户配置。当前入口静态估算约 3149 token、治理 references 合计约 11077 token，仅作体积观测，不宣称每轮成本下降。未新增真实模型运行，也不把下面各历史快照的成绩当作新版本重复实测。

治理 `0.7.0` 重构（contract `9`、编排 `0.2.0`、技能演进退役、未配置职责映射时不暴露职责缺口工具与委派指引）通过 `check:dsh`、canonical 资源校验、版本策略 3/3 与双包版本一致性检查、离线评测设施自检 13/13、Hooks 与路由安装测试、打包生命周期 20/20，以及 runtime、Plugin、Agent 与 Web RPC 共 301 项行为测试；删除的 16 项只覆盖已退役的演进，新增 1 项锁定职责缺口工具的暴露条件，另有一项断言部署配置残留 `evolutionRoot` 时仍可加载。双包 `npm pack` 与制品校验通过，产物含 `memory.md` 与 `install.md`、不含 `support.md` 或演进模块，六个生成目录均已清理。提交前审查补清 craft 的 R11 内部强不变量边界、演进迁移条件和 C19 证据表述，并在既有兼容测试中补上 schema 5 / contract 8；合同组合与来源选择 16/16 通过。发布矩阵首跑发现 Agent 与共存探针仍要求默认三个工具，已按新的无映射/路由关闭行为修正，并在 Agent 真实宿主探针补验映射新增、移除后下一轮的工具、执行目录与委派指引同步。修正后的 `verify-dsh-release-matrix.mjs` 全部通过：精确 rc.2 的 231 个 DSH 包依赖图、Standard 身份、历史会话不改写、Plugin 加载、Agent 权限与动态工具暴露、双包打包及共存。上述正文和文档收尾复用未受影响的程序证据，未新增付费烟测或模型运行。随后 registry 核对确认 npm `0.2.36` 已发布，源码候选纠正为 `0.2.37`；此次仅改包版本、兼容元数据与发布归属文档，治理、编排和 runtime 源码未变。`0.2.37` 的版本策略与发布入口测试 20/20、canonical 校验、双包实际打包和制品字节一致性已重验通过；前述行为测试与源码发布矩阵按未受影响属性复用，不将它们记为 npm `0.2.36` 制品的实测。

当时仍标为 `0.2.36` / 治理 `0.6.1` 的未发布源码快照（不是 npm `0.2.36` 制品）在本轮测试减负之前完成死代码清理和记忆引用误捕获修复，通过 `npm run check:dsh`，以及 `npx tsx --test dsh/runtime/tests/*.test.mts dsh/plugin/tests/*.test.mts scripts/dsh-web-rpc.test.mjs dsh/agent/tests/*.test.mts` 的 319/319 项回归。版本策略测试 3/3、canonical 资源校验、双包 `pack:dry-run` 和全部六个生成目录清理检查通过。记忆回归覆盖引号内示例、句号、带引号的偏好值、英文缩写，以及引用内容不能扩大作用域、改变类别或使冲突候选取代既有记录；只证明这些自动提取边界，不证明任意转述都能识别。没有修改既有用户记忆，也未将这些程序检查计为模型 A/B 或行为收益证据。

评测 harness 的七项测试均已通过（重裁测试修正调用参数后定向复跑），覆盖完整包指纹和路由身份不匹配时拒绝复用；无模型调用。`test-odai-routing.mjs` 覆盖三个宿主安装/更新/卸载的临时目录清理、目录来源、非托管文件、旧清单、越界记录与配置恢复；`test-odai-hooks.mjs` 覆盖 NotebookEdit 三种编辑模式的路径保护及 Claude matcher，并保持普通 shell 不在覆盖范围内。两项脚本通过；现有宿主安装仍需重新生成或更新，不能把源码修复当作已部署。

随后按用户决定削减测试负担：删除固定提示措辞和 token 估算门槛，harness 不再默认选全量或在单题前执行整套自检，模型评测与离线评测设施检查改为显式按需入口。此次通过 `check:dsh`、八个受影响测试文件的 153 项回归，以及显式设施入口的离线自检和 13 项测试；未重跑无关全量、真实模型或发布矩阵，上述历史验证仍只属于各自快照。

普通测试改为 JavaScript 后，`check:dsh` 保留生产源码严格类型检查；原生 `node --test` 运行 runtime、Plugin、Agent 与 Web RPC 共 316 项全部通过。三个 `.mjs` 宿主探针在显式指定本机 rc.2 `DSH_PACKAGE_ROOT` 后通过，覆盖历史会话不改写、Plugin 加载和 Agent 权限/工具暴露；未调用真实模型，也未重新运行发布矩阵或付费烟测。

此前遗漏的旧保护预期、依赖正文的演进夹具及测试类型收窄错误已修正。回归继续确认活动职责受保护、历史记录不改写、被拒绝的替换不生成候选或改变激活状态；这些修正未放宽运行时守卫。

`7d2ade5` 已完成 `node scripts/verify-dsh-release-matrix.mjs`：231 个 rc.2 包构成纯依赖图，Plugin 加载、Agent 权限保护、双包打包与共存均通过。真实宿主验证了可选 goal/todo/Ralph 的提示适配、编排按需入口、reviewer 补读源码与不可变证据并存，以及写入和 shell 禁止。后续沟通原则恢复、夹具修正及制作、验证、交接规则精简已通过前述 CI 入口；未重跑该矩阵，不将旧结果记成新快照的重复实测。

旧版本的隔离加载与制品记录保留在 CHANGELOG 和 Git 历史中，不自动转为当前验收。2026-09-21 的治理 `0.6.0` 定向结果见 [`evaluation-results.md`](evaluation-results.md)；这些历史评分不证明 `0.6.1` 的模型效果。

协作相对收益仍需同版本、同题面、同宿主与模型条件下的 B/C 对照及实际调用、usage 证据；单独成功的 C 臂或没有发生委派的样本均不能证明收益。当前没有据此宣称 token、延迟、费用或长期模型质量改善。

## 0.6.1 托管编排开关试验

DSH `0.2.36` / 治理 `0.6.1` / 编排 `0.1.0`，宿主 `0.1.5-rc.2` Web/standard；总控、职责映射与独立只读裁判均为 `openai/gpt-5.6-sol/xhigh`。两组使用同一冻结源码和题本，B 为 source-plugin/on、托管编排 off，C 为相同配置下的 auto；没有修改用户路由存储。

| 题目 | B：托管编排关闭 | C：按需托管编排 |
|---|---|---|
| C04 | 2/4；55.6 秒；45,569 tokens；无子会话 | 4/4；92.3 秒；68,337 tokens；无子会话 |
| C20 | 900 秒超时，未评分；2 个原生子会话 | 900 秒超时，未评分；2 个托管 reviewer 子会话 |

C04 的 input/cache-read/output 分别为 B `16,315/27,648/1,606`、C `19,406/46,592/2,339`；时长为 runner wall time，token 含缓存读取、不含裁判。C20 原生记录确认所有会话均使用上述模型与推理档位，C 另有 `odai/route-applied` 和父子回执，证明职责实际调用。超时样本没有完整总用量，不能用于完整成本比较。未取得实际账单或适用价格，不估算金额。

**没有建立编排净收益或严格“总控独做／分工”对照。** B 仍调用了原生子代理；C04 没有实际委派；C20 两组均未完成。单次评分差异、缓存量和耗时差异不作因果或稳定性结论。

原始证据在仓库外 `../odai-evaluation-runs/candidate-0.6.1-question-bc/`：`study.json`、`frozen-fixed/`、`B-fixed/`、`C-fixed/`、`B-rejudged/`。首次两次 C04 因采集代码误读原生回执字段而停止，原件保存在 `capture-failure/`，不计分；修正后两份真实提问均成功采集。B 首次裁判遭遇 `stream_read_error`，只对同一冻结输出重裁，采用 `B-rejudged/C04/judge.json`；C 采用 `C-fixed/C04/judge.json`。共六次任务运行、三次裁判调用，失败记录保留，不按最好结果挑选样本；本轮实验已结束，不继续追加。

## 0.3.15 冻结模型评估

2026-09-17 使用 `openai/gpt-6-astra/xhigh` 总控，对比：A 干净 DSH/off；B source-plugin/on、managed routing off；C 在B基础上启用用户原样职责配置。只改变声明的治理/路由条件，不修改用户配置或安装中的 DSH。

| 职责 | 用户配置 |
|---|---|
| researcher | `openai/gpt-5.6-luna/max` |
| planner | `openai/gpt-6-astra/xhigh`，child |
| reviewer | `openai/gpt-5.6-terra/xhigh`，child；快照未显式设置maxTokens，实际回执为128000 |
| frontend | `kimi-coding/k3/max`，child |

C01、C04、C08的C臂各得4/4，原生记录均只有总控会话，没有借角色调用凑流程。这证明这三次任务可以保持直接完成，不证明配置职责的净收益。

C20按用户要求重试后仍超过900秒。最新完整原生记录包含总控与两个 `openai/gpt-5.6-terra/xhigh` reviewer子会话，路由回执核实maxTokens 128000；一次完成回交、一次fallback。完成回交仍因关键API、worker、database、回退或相关diff缺失/截断而未独立放行。总控有本地测试通过的证据，并保留生产门禁；这仍不等于任务完成。当前材料已替换对应旧结果，不再维护平行失败目录。

这一观测确认了真实调用与材料交接瓶颈，**没有证明协作净收益**。A-C20有三个原生会话，B-C20只有一个；managed routing off也可以出现原生委派，不能把A/B称为纯单代理对照。调用次数、缓存与时序不同，不从一次token差异推导费用或稳定性结论。

## 0.3.16 证据补读修复与验证

默认摘要仍为12000字符、单项最多4000，并受剩余预算分配影响。`dsh/runtime/src/routing-context.mts`现在在裁剪前保留当前任务中已核验调用归属的原生工具结果，交给`review-evidence.mts`提供不可变快照。托管reviewer可按摘要中的快照摘要值和来源标识补读必要页；源身份与原文SHA-256随页返回。

快照保留最近最多500条工具记录和200万字符，省略数量与单项截留情况明示；每次回交周期最多40次成功请求、64000个响应JSON字符，每页正文最多4000字符。初始摘要预算与用户模型配置不变。工具绑定实际父会话及唯一托管reviewer子会话，结束后撤销；总控、其他职责、复制标签的子会话均不能读取。该reviewer只可调用证据分页工具，文件扫描、命令、联网和继续委派在执行层被拒绝。

验证包括304项runtime/Plugin/Agent回归、严格类型检查及隔离DSH加载/执行检查。真实宿主检查通过原生工具注册器补读第9000字符后的内容，确认文件读取被拒绝和结束后失效。对上述C20最新超时原生记录回放时，快照识别77条工具证据；一条10240字符的退款worker相关证据经3页恢复，重组SHA-256与原捕获一致。这证明修复了包内裁剪后的补读路径，不是新一轮模型评分或C20任务通过。

分页只恢复宿主已经捕获的内容。原工具结果本身截断、从未读取的新文件、快照截留或预算耗尽，仍须明确回交；可用页不等于验收通过。本轮未重跑两项超时任务，也未改变C20评分；协作质量、延迟和费用的普遍净收益仍未建立。

## 局部动作保护边界

当前风险保护与能力选择分开：高影响前提先要求证据，不自动制造planner；真实职责缺口按用户映射处理。同步tool guard尚不能把预检查的路径绑定到后续实际写入，因此不能声称已有安全的文件级局部放行。

宿主本身已有目标绑定原语：DSH `dsh-tool-fs/lib/index.js` 将同一 `FsTarget` 和execution actor送入 `fs/write-intent` / `fs/edit-intent`，再使用该target调用实际 `ctx.fs` 写入；`dsh-fs/lib/types/index.d.ts` 定义single-slot waterfall。新的保护集成仍需验证监听顺序、组合、目标身份和撤销；shell及自定义工具副作用不会自动受这些钩子覆盖。本轮没有新增路径例外或降低已有保护。

## 0.2.33 执行策略与 rc.2 验证

当前候选保持已有职责预设、模型映射和 dispatch 配置，执行权限从真实 scope 的 continuation policy、child 身份与风险保护求得；同一个判断供工具目录和提示过滤使用，并与执行守卫共用权限来源。相同模型的 planner 与 frontend 不因此获得相同权限，待派发 gap 本身也不改变总控权限。

新增行为回归覆盖四种同线程职责、目录限制可用/缺失/抛错三种条件，以及宿主先组装后路由、浅复制提示对象、职责回交后的首个总控请求。发现并修复同线程 reviewer 因缺少 child-only 证据包而回交后重复启动；独立 child 缺包仍等待证据，同线程结果不升级为独立验收。子代理移除不适用的总控输出提示，保留职责 owner、真实请求预算和证据权限。

`node scripts/verify-dsh-release-matrix.mjs` 已在隔离目录完成 rc.2 验证：231 个 DSH 包版本一致，Standard 摘要匹配；Plugin 加载、原生工具执行保护、Agent scope 首次权限切换与恢复、双包制品共存、Control Center RPC 和项目 skill 选择均通过。验证没有改动上游 DSH 或用户运行环境。rc.2 的 spawn 接口提供工具过滤，但当前没有任务级文件写域隔离的完整证据，因此继续保留 child 只读。

以上是软件行为与宿主兼容性证据，没有新增真实模型 A/B、缓存命中、延迟或费用对照，不将减少提示内容或避免重复路由表述为已测 token/成本收益。

## 当前可用性

已发布的 0.2.32 包含原生调用卡片布局和缺失轮次标签修复，其隔离浏览器验证记录保持。0.2.33 已发布，当前已发布双包为 0.2.37，源码候选为 0.2.38；源码修改不表示正在运行的 GUI 已更新。回执只证明调用或模型身份，不证明任务完成。

## 历史测评数据

保留截至2026-09-04的有效测评数据供比较；旧版配置说明及执行计划已移出当前说明。这些映射、预算、价格和实现只描述当时的冻结实验，不是当前默认配置或当前版本验收。金额为当时价格假设下的估算，缓存与上下文档位会影响结果。

### Researcher与frontend

C04 researcher的四份独立样本均使用Luna/xhigh/500与Sol/xhigh/500，均取得三源packet；C01/C05负向样本未触发researcher。新增三轮runner/judge均通过隔离校验。

| C04样本 | 分数 | input / cached / output | 墙钟 | runner成本 | judge token |
|---|---:|---:|---:|---:|---:|
| 纯Sol基线 | 4/4 | 24,030 / 101,376 / 2,289 | 未记录 | $0.239508 | 未记录 |
| Luna researcher + Sol，初始 | 4/4 | 43,101 / 119,296 / 3,910 | 87.6s | $0.154871 | 未记录 |
| 同路由复跑1 | 4/4 | 41,895 / 68,608 / 4,445 | 98.5s | $0.183011 | 27,458 |
| 同路由复跑2 | 4/4 | 44,962 / 155,136 / 3,314 | 86.0s | $0.203505 | 27,467 |
| 同路由复跑3 | 4/4 | 55,841 / 65,536 / 3,880 | 88.2s | $0.224032 | 27,700 |

当时每百万input/cached/output采用Sol $5/$0.50/$30、Luna $0.20/$0.02/$1.20。researcher四轮成本合计$0.765420、均值$0.191355，墙钟均值90.1s；相对单份Sol基线均值低20.11%，但基线n=1，不构成通用或统计稳定降本结论。provider均超过请求500 ceiling，不代表硬上限。

| 样本 | 实际模型 | 分数 | runner token | input / cached / output | 墙钟 | 成本 |
|---|---|---:|---:|---:|---:|---:|
| C08 frontend，同turn | K3/max/4096 | 4/4 | 98,619 | 10,952 / 83,968 / 3,699 | 133.9s | 无权威单价 |
| C01简单查询 | Luna/max，未调用其他职责 | 4/4 | 72,662 | 非缓存输入+输出11,222 | 52.8s | $0.0044 |

C08形成于canonical 0.2.0，后续0.2.1未触及该题合同；只新增交接文档，真实header证明同turn升级，不是child，也不证明frontend普遍优于总控。

### C04冻结对照

八臂使用同一C04与独立Sol/high judge，各一份、不best-of。字母只标识这组历史实验，与上文当前A/B/C无关。

| 臂/路线 | 实际模型链路 | 分数 | critical | runner token | 墙钟 | 估算成本 | diff |
|---|---|---:|---|---:|---:|---:|---:|
| A DSH/off | Luna/max | 0/4 | 是 | 117,245 | 101.9s | $0.0069 | 1 |
| B Plugin/routing off | Luna/max | 4/4 | 否 | 137,066 | 143.0s | $0.0135 | 0 |
| C Plugin/routing off | Sol/high | 4/4 | 否 | 89,622 | 68.5s | $0.1604 | 0 |
| D Plugin/execute | Luna → Sol child → Luna | 4/4 | 否 | 153,607 | 137.7s | $0.1156 | 0 |
| E Plugin/旧observe | Luna，planner命中但未spawn | 2/4 | 否 | 189,186 | 189.8s | $0.0201 | 0 |
| F Codex+Odai | Sol/high | 4/4 | 否 | 118,219 | 83.9s | $0.2061 | 0 |
| G Agent/旧observe | Luna，planner命中但未spawn | 1/4 | 是 | 155,646 | 94.5s | $0.0125 | 1 |
| H Agent/execute | Luna → Sol child → Luna | 4/4 | 否 | 211,252 | 160.0s | $0.2211 | 0 |
| H复跑 | Luna → Sol child → Luna | 4/4 | 否 | 229,946 | 128.8s | $0.2756 | 0 |
| Agent/auto | 同一controller turn直接Sol | 4/4 | 否 | 90,193 | 65.6s | $0.2072 | 0 |
| 修复后Plugin/observe | Luna，本地证据协议+只读保护 | 4/4 | 否 | 130,243 | 108.8s | $0.0142 | 0 |

D/H具有child durable header与actualRoute；B/C表明治理或单一强总控也可能足够。旧E/G暴露的保护缺口后续得到修复。auto减少了这组样本的重复上下文处理，但不证明所有任务更省、更快或更稳。

### 退役方案的对照依据

| 场景 | 路线 | 结果 | runner token | 墙钟 | 估算成本 |
|---|---|---|---:|---:|---:|
| 12文件迁移 | Sol/high → Luna/max executor | 目标改动及测试通过 | 218,731 | 222.9s | $0.213 |
| 同题 | 单Sol/high | 同等改动及验证 | 137,261 | 63.6s | $0.201 |
| 19题强制前置 | Luna + 每题Sol前置 + 按需执行/审查 | 144/144 | 7,201,838 | 39.6m | $5.512 |
| 同题 | 单Sol | 144/144 | 3,698,792 | 31.1m | $6.005 |
| 前代stage退款 | 单Sol/high | 4/4 | 326,960 | 240.7s | $0.788 |
| 同题 | Sol/high定路 → Luna/max实施 | 4/4 | 325,249 | 390.5s | $0.212 |

强制前置/单Sol的非缓存输入+输出分别为1,074,222与554,088。独立Executor迁移增加59.4% token、约3.5倍墙钟、6.0%估算成本，是退役依据；stage退款成本较低但延迟增加62.2%。这些数据不支持恢复已退役的Executor、route card或stage机制。
