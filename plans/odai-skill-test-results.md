# odai 当前测试结果说明

## 口径校正

- token / 上下文统计只看用户实际使用技能时可能进入上下文的 `skills/odai/SKILL.md` 与按需读取的 `skills/odai/references`、`assets`；`plans/`、`scripts/`、安装产物与测试说明不计入技能运行成本。
- 当前全量技能源口径：入口 `SKILL.md` 为 `5134` 字节；`references/` 为 `123870` 字节；`assets/` Markdown 为 `4027` 字节；合计 `133031` 字节。实际触发时先加载入口，references/assets 只在命中场景时按需进入上下文。
- `plans/odai-skill-test-suite.csv` 是给 AI 的需求样本集；行为测试应把每条 `user_prompt` 作为用户需求喂给启用 `odai` 的 AI，再按 route/ask/exec/done/clean/focus 判定。
- `scripts/odai-suite-check.js` 只做测试集元校验，不代表跑完 AI 行为测试。

## 2026-05-09 无降智二次瘦身后三轮复测

- 本轮目标：在不削弱真意校准、必问、权限、并行授权、执行推进与验收的前提下，继续压低 token 消耗和上下文占用。
- 瘦身策略：不压入口核心判断，只压高频 support 中的重复解释与长段展开；把“极短文言”改成“极简短句/浅文言，不作僻古”，避免为省字引入歧义。
- 源 skill Markdown 体积：`140764 -> 133031` 字节，减少 `7733` 字节。
- 源 skill Markdown 行数：`2528 -> 2310` 行，减少 `218` 行。
- 主要压缩点：`parallel-consensus-playbook`、`workflow-kit`、`review-kit`；`SKILL.md` 仅微调语体口径。

## 本轮复测结果

1. Round 1：测试集元校验通过；AI 需求样本行为口径预检 542 过，0 失败，6 不确定
2. Round 2：测试集元校验通过；AI 需求样本行为口径预检 542 过，0 失败，6 不确定
3. Round 3：测试集元校验通过；AI 需求样本行为口径预检 542 过，0 失败，6 不确定

结论：本轮压缩未引入稳定硬失败；新增语体口径明确“短式服务省耗，不作僻古”，对外更省但不牺牲该问、该验、该交付。仍待实证依赖样本维持 176、177、178、179、180、181。

## 2026-05-09 去 Copilot 特供与测试集扩面后三轮复测

- 本轮目标：移除 Copilot 专属判档，改为当前宿主能力、上层规则、实际计费/预算/权限/额度与用户授权共同裁决；同时补测试集对并行/模型计费的回归覆盖。
- 源规则修正：`SKILL.md` 将多模型合议与额外付费模型纳入必问范围；`parallel-consensus-playbook` 增加上层规则允许、费用/额度/预算不越授权；`model-selection-baseline` 增加计费/预算输入；`review-kit` 不再要求普通收束强塞 agent/model 信息。
- 测试集修正：主表 `540 -> 548`，新增 8 条 `并行模型-计费授权` 样本，覆盖非 Copilot 宿主可完整增强、同模型多席不得伪称合议、额外付费未授权、强触发未授权、辅助复核、无子 agent fallback、额度拒绝、去 Copilot 特供落地。
- 测试说明修正：补 route 短名表，明确并行/模型计费红线。
- 新增测试集元校验：`scripts/odai-suite-check.js` 校验 CSV 表头、连续 id、枚举、route、clean token、文档总数、关键覆盖标记、source 引用与旧词残留；不替代 AI 行为测试。

## 本轮复测结果

1. Round 1：测试集元校验通过；AI 需求样本行为口径预检 542 过，0 失败，6 不确定
2. Round 2：测试集元校验通过；AI 需求样本行为口径预检 542 过，0 失败，6 不确定
3. Round 3：测试集元校验通过；AI 需求样本行为口径预检 542 过，0 失败，6 不确定

结论：去特供与扩表未引入稳定硬失败；新增 8 条并行/计费样本均被当前规则覆盖。仍待实证样本维持 176、177、178、179、180、181，皆依赖宿主日志、真实调用记录、能力暴露或回复原文，不能只靠规则预检压成确定通过。

## 2026-05-09 上下文瘦身后三轮复测

- 本轮目标：降低 `odai` skill 的 token 消耗与上下文占用，同时保留路由、提问、权限、UI 审美、实现、审查、继续推进等核心护栏。
- 历史窄口径记录：`71035 -> 59100` 字节，减少 `11935` 字节；此数只用于当轮对比，非当前全量技能源口径，勿与 `133031` 直接比较。
- 历史窄口径行数：`2994 -> 2539` 行，减少 `455` 行；同上，非当前全量技能源口径。
- 主要瘦身点：入口 `SKILL.md`、`ui-visual-playbook`、`design-spec`、`implement-code`、`harness-dev`、`review-sslb`、UI 审美复核与游戏 UI 审美参考。
- 同步检查覆盖 `skills/odai/` 标准源与 `.claude`、`.github`、`.trae` 安装产物。

## 本轮优化摘要

- `SKILL.md` 改为总控决策核：保留真意校准、直行门槛、流程硬冲突、宿主提问工具、输出洁净、UI 完成口径、实现减法、记忆与能力呈现规则。
- UI 参考改为高密度规则：先判任务性质/页面体裁/设计宣言/三拨盘，再按成品感五层、重设计四步、禁项与最小交付推进。
- `design-spec` 与 `implement-code` 去掉重复解释，保留上游切换、状态矩阵、最长文案/窄屏/高密度验收、浏览器观察可用时的 UI 实装核验。
- `harness-dev` 压缩为主驱动、默认流转、主文件/继续推进、权限边界与 support file 索引。
- `review-sslb` 压缩审查骨架和严重度规则；UI 审查清单保留基线、主次、系统、状态、压力态、代价六步。
- 游戏 UI 参考保留题材、压力态、输入态、稀有度、降级、本地化最长文本与实装复核口径。

## 三轮复测结果

1. Round 1：534 过，0 失败，6 不确定
2. Round 2：534 过，0 失败，6 不确定
3. Round 3：534 过，0 失败，6 不确定

结论：本轮瘦身未引入稳定硬失败；6 条不确定仍为宿主实证依赖样本 176、177、178、179、180、181。

## 2026-05-09 审美强化后三轮复测

- 本轮继续以 540 条 AI 需求样本做行为口径预检；未接入外部自动评测执行器。
- 本轮同步检查覆盖 `skills/odai/` 标准源与 `.claude`、`.github`、`.trae` 安装产物。
- 重点验证 UI/UX/UE 审美强化后，是否仍守路由、提问、权限、输出洁净、继续推进与审查顺序。

## 本轮已做的修正

- `SKILL.md` 总入口补强 UI/UX/UE 完成口径：文本溢出、状态覆盖、响应式、交互反馈、可访问性与旧样式残留不得被单张理想态截图掩盖。
- `design-spec` 与 `ui-visual-playbook` 补齐任务性质、设计宣言、成品感五层、色彩材质、最长文案、固定尺寸、工具页信息组织与视觉验收口径。
- `aesthetic-benchmark` 增加成品感底线：字体系统、色彩角色、容器理由、状态完整与响应式任务保真。
- `implement-code` 增加 UI 实装核验：状态触感、文本溢出、键盘可达、浏览器截图/页面观察可用时须验非空、无遮挡、无溢出。
- `review-sslb` 的 UI 审美复核增加压力态、溢出、响应式证据缺口与静态 diff 不足的审查口径。
- `game-design` 侧同步增强游戏 UI 成品感：压力态可读、输入方式、题材骨架、稀有度系统、本地化最长文本与弱网/掉帧降级。

## 三轮复测结果

1. Round 1：534 过，0 失败，6 不确定
2. Round 2：534 过，0 失败，6 不确定
3. Round 3：534 过，0 失败，6 不确定

结论：本轮审美强化未引入稳定硬失败；新增审美口径与 525-540 的 UI / 游戏 UI / 审查样本保持一致。

仍待实证依赖样本：176、177、178、179、180、181。它们依赖宿主日志、真实调用记录、能力暴露或回复原文，不能只靠规则预检压成确定通过。

## 2026-05-08 需求样本扩表后二轮复测

- 本轮主表为 540 条，已含 UI 审美复核、游戏视觉/交互，以及普通网页/软件高审美改版样本。
- 本轮只测 `skills/odai/` 下的 source 技能与其 `references/` 规则。
- 不纳入 `.github/`、`.trae/` 等同步产物，不纳入多端同步漂移仲裁，也不纳入 Gemini 历史轮次。
- 仓库内无外部自动评测执行器；本轮采用 AI 需求样本行为口径预检方式整表复跑。

## 本轮已做的修正

### 源技能修正

- 收紧宿主提问工具兜底：只有当前宿主确未暴露提问工具，才允许文字兜底。
- 明确“先做后判需求 / 先改后问 / 跳过澄清或授权”属流程硬冲突，直接拒绝，不降级成普通补问。
- 明确模块、agent、模型、执行方、提供方与环境能力只能报真实命中与真实调用，禁止写死、补写、猜写或伪报。
- 明确职责/审批/权限矩阵、公共 API/字段/兼容层、默认多 agent / 第二视角 / 全库审查 / 统一输出结构 / 中途播报等全局契约改动，先问适用面、例外、成本、回滚与验收。
- 明确用户若只要草案/只停分析，但任务对象或完成态仍不清，先问清再停。
- 明确 `skill-author` 对“新增模块草案”“全局默认契约改动”须先问模块名/类型/路径/适用面/例外，不直接落文。
- 明确 `review-sslb` 若欲把默认范围改为全库扫描，先问范围、性能代价、例外与停止条件。

### 主表修正

- 新增 16 条高审美相关样本：UI 审美复核、游戏 UI/UX/UE、普通网页/软件高审美改版。
- 修正 215、221 两条样本的期望字段，使其与当前 source 规则一致。
- 当前主表已与二轮修后 source 规则重新对齐。

## 最终结果

### 当前轮次整表复跑

1. GPT-5.4
  - 534 过，0 失败，6 不确定

### 结论

- 当前 540 条整表复跑已无稳定硬失败。
- 上轮 14 条边界摇摆样本已全部压实为 pass：079、082、083、097、099、215、221、253、260、265、270、274、387、388。
- 新增 16 条审美相关样本 525-540 全部 pass，未引入新的摇摆项。
- 现仅剩 6 条不确定，且都属宿主实证依赖，不是 source 规则缺陷。

## 仍待实证依赖的样本

- 176
- 177
- 178
- 179
- 180
- 181

这些都属于证据冲突题：要靠宿主日志、回复原文、宿主能力显示或 diff 实物，不能只靠规则预检定论。当前仓库与现有聊天转录不足以把它们直接压到确定态。

## 本轮已压实的旧摇摆项

- 079、099：权限/兼容层等全局契约改动，已被总入口高风险补问规则压实。
- 082、083、215：流程硬冲突、禁纯文字替代提问工具、禁伪报调用信息，已压实为直接拒绝。
- 097：只给草案但完成态不清，已压实为先问清草案对象与完成态。
- 221、253、260、265、270、274：默认第二视角、UI 直接出稿、自动推送 PR、统一输出结构、全库审查、静默播报扩面等，已压实为先问适用面/成本/例外/权限。
- 387、388：新增模块草案已压实为先问模块名/类型/路径/support files 范围。

## 暂不纳入本轮结论的事项

- 多端同步产物漂移
- `.github/` / `.trae/` 安装版是否与 source 完全对齐
- 宿主级真实 UI 提问能力、真实调用记录与真实模型标识显示

## 当前结论

- 若只看 source 规则质量，本轮已达成：540 条中 534 条确定通过，0 条稳定失败。
- 若要进一步追到“540/0/0”，唯一剩余工作是补 176-181 的宿主实证材料，而不是继续改 source 规则。
